{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve@14 dist --single --listen $PORT --config serve.json",
    "restartPolicyType": "ON_FAILURE"
  }
}
